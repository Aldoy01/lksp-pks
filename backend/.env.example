# Salin file ini ke .env dan isi nilainya
# cp .env.example .env

# ── Wajib diubah di production ──────────────────────────────────
# Generate dengan: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=GANTI_DENGAN_STRING_RANDOM_PANJANG_64_KARAKTER

# ── Database ────────────────────────────────────────────────────
# SQLite (default, cocok untuk MVP)
DATABASE_URL=sqlite:///./iksp.db

# PostgreSQL (untuk production yang lebih robust)
# DATABASE_URL=postgresql://user:password@host:5432/iksp_db

# ── CORS ────────────────────────────────────────────────────────
# Isi dengan URL domain frontend Anda (pisah koma jika lebih dari satu)
# Contoh Railway: https://iksp-production.up.railway.app
# Contoh custom domain: https://iksp.pks.id
CORS_ORIGINS=https://iksp.pks.id,https://www.iksp.pks.id

# ── Token expiry ─────────────────────────────────────────────────
ACCESS_TOKEN_EXPIRE_MINUTES=480
